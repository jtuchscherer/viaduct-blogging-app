# Copy this file to .env and fill in real values before running start-containers.sh
# .env is gitignored — never commit it.

POSTGRES_DB=blog
POSTGRES_USER=blog
POSTGRES_PASSWORD=change-me

JWT_SECRET=change-me
CORS_ORIGIN=localhost:5173
